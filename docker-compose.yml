services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-wordsniffer}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wordsniffer}
      POSTGRES_DB: ${POSTGRES_DB:-wordsniffer}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-wordsniffer}"]
      interval: 5s
      timeout: 5s
      retries: 5

  python-api:
    build:
      context: ./artifacts/python-api
      dockerfile: Dockerfile
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-wordsniffer}:${POSTGRES_PASSWORD:-wordsniffer}@postgres:5432/${POSTGRES_DB:-wordsniffer}
      PORT: ${PORT:-8000}

volumes:
  pgdata:
